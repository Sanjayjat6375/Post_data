  <!-- Navbar -->
  <nav class="bg-white shadow-lg fixed top-0 w-full z-10">
    <div class="max-w-6xl mx-auto px-4">
      <div class="flex justify-between">
        <!-- Logo and Primary Navbar items -->
        <div class="flex space-x-7">
          <div>
            <!-- Website Logo -->
            <a href="/listings" class="flex items-center py-4 px-2">
              <svg xmlns="http://www.w3.org/2000/svg" fill="none" stroke="currentColor" stroke-linecap="round"
                stroke-linejoin="round" stroke-width="2" class="w-10 h-10 text-white p-2 bg-indigo-500 rounded-full"
                viewBox="0 0 24 24">
                <path d="M12 2L2 7l10 5 10-5-10-5zM2 17l10 5 10-5M2 12l10 5 10-5"></path>
              </svg>
            </a>
          </div>
          <!-- Primary Navbar items -->
          <div class="hidden md:flex items-center space-x-1">
            <a href="/listings" class="py-4 px-2 text-green-500 border-b-4 border-green-500 font-semibold">Explore</a>
          </div>
        </div>
        <!-- Secondary Navbar items -->
        <div class="hidden md:flex items-center space-x-3">
          <form action="/search" method="GET" class="flex gap-4">
            <input type="text" name="q" class="border text-center px-4 rounded-[20px] py-1"
              placeholder="Search destinations">
            <button type="submit" class="bg-red-500 hover:bg-red-600 text-white font-semibold rounded-[20px] px-4 py-1">
              <i class="fas fa-search"></i> Search
            </button>
          </form>
        </div>
        <div class="hidden md:flex items-center space-x-3">
          <a href="/listings/new"
            class="py-2 px-2 font-medium text-gray-500 rounded hover:bg-green-500 hover:text-white transition duration-300">Airbnb
            your home</a>
          <% if (!currentUser) { %>
            <a href="/signup"
              class="py-2 px-2 font-medium text-gray-500 rounded hover:bg-green-500 hover:text-white transition duration-300">Sign
              up</a>
            <a href="/login"
              class="py-2 px-2 font-medium text-gray-500 rounded hover:bg-green-500 hover:text-white transition duration-300">Login</a>
            <% } else { %>
              <a href="/logout"
                class="py-2 px-2 font-medium text-gray-500 rounded hover:bg-green-500 hover:text-white transition duration-300">Logout</a>
              <% } %>
        </div>
        <!-- Mobile menu button -->
        <div class="md:hidden flex items-center">
          <button id="mobile-menu-button" class="outline-none mobile-menu-button">
            <svg class="w-6 h-6 text-gray-500 hover:text-green-500" fill="none" stroke-linecap="round"
              stroke-linejoin="round" stroke-width="2" viewBox="0 0 24 24" stroke="currentColor">
              <path d="M4 6h16M4 12h16M4 18h16"></path>
            </svg>
          </button>
        </div>
      </div>
    </div>
    <!-- Mobile menu -->
    <div class="mobile-menu">
      <ul>
        <li><a href="/listings" class="block text-sm px-2 py-4 text-white bg-green-500 font-semibold">Explore</a></li>
        <li>
          <form action="/search" method="GET" class="flex gap-4 p-4">
            <input type="text" name="q" class="border text-center px-4 rounded-[20px] py-1 w-full"
              placeholder="Search destinations">
            <button type="submit"
              class="bg-red-500 hover:bg-red-600 text-white font-semibold rounded-[20px] px-4 py-1 w-full">
              <i class="fas fa-search"></i> Search
            </button>
          </form>
        </li>
        <li><a href="/listings/new" class="block text-sm px-2 py-4 hover:bg-green-500 transition duration-300">Airbnb your home</a></li>
        <% if (!currentUser) { %>
          <li><a href="/signup" class="block text-sm px-2 py-4 hover:bg-green-500 transition duration-300">Sign up</a></li>
          <li><a href="/login" class="block text-sm px-2 py-4 hover:bg-green-500 transition duration-300">Login</a></li>
        <% } else { %>
          <li><a href="/logout" class="block text-sm px-2 py-4 hover:bg-green-500 transition duration-300">Logout</a></li>
        <% } %>
      </ul>
    </div>
  </nav>

  <script>
    const btn = document.getElementById("mobile-menu-button");
    const menu = document.querySelector(".mobile-menu");

    btn.addEventListener("click", () => {
      menu.classList.toggle("active");
    });
  </script>
  <script src="https://kit.fontawesome.com/a076d05399.js"></script>

